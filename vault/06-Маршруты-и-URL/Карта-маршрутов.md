# Карта маршрутов

> Источник: `src/router/index.js`. **Обновлено тикетом 139**:
> `/admin-busses`/`/children`/`/headcount` удалены тикетом 137 (см.
> [[Учёт-автобусов]], [[useChildPresence]] — исторические); единственная
> точка входа администратора теперь — кнопка «Checkpoints» в
> `MainView.vue` → `/admin/checkpoints/*` (см. [[Checkpoint]]).
> `/scanner` (единый экран) заменён тремя отдельными маршрутами тикетом
> 120. **Обновлено тикетом 151**: `/child/:id` и
> `/admin/checkpoints/child/:id` разведены по единственной, не
> пересекающейся ответственности (см. строки ниже) — все ссылки на
> ребёнка из Checkpoint-экранов (`ChildLink.vue`) теперь ведут на
> `/child/:id`, а не на `/admin/checkpoints/child/:id`.

| Path | Name | meta | Компонент |
|---|---|---|---|
| `/login` | Login | `public: true` | `LoginView.vue` |
| `/welcome` | Welcome | `public: true` | `WelcomeView.vue` |
| `/` | — | redirect → `/main` | — |
| `/main` | Main | `requiresAuth: false` | `MainView.vue` |
| `/armband/:id` | Armband | `requiresAuth` | `ArmbandView.vue` (см. [[Идентификация-ребёнка]]) |
| `/info` | Info | `requiresAuth: false` | `InfoView.vue` — публичная страница |
| `/group-edit/:id?` | GroupEdit | `requiresAuth` | `GroupEditView.vue` |
| `/select-child` | SelectChild | `requiresAuth` | `SelectChildView.vue` |
| `/scanner/bus` | ScannerBus | `requiresAuth` | `ScannerBusView.vue` (тикет 120) |
| `/scanner/group` | ScannerGroup | `requiresAuth` | `ScannerGroupView.vue` (тикет 120) |
| `/scanner/checkin` | ScannerCheckin | `requiresAuth` | `ScannerCheckinView.vue` (тикет 120) |
| `/scanner/settings` | ScannerSettings | `requiresAuth` | `ScannerSettingsView.vue` |
| `/admin/checkpoints` | CheckpointList | `requiresAuth, requiresAdmin` | `CheckpointListView.vue` (тикет 134, см. [[Checkpoint]]) |
| `/admin/checkpoints/bus/:id` | CheckpointBus | `requiresAuth, requiresAdmin` | `CheckpointBusView.vue` (тикет 134) |
| `/admin/checkpoints/group/:id` | CheckpointGroup | `requiresAuth, requiresAdmin` | `CheckpointGroupView.vue` (тикет 134) |
| `/admin/checkpoints/lazy/:id` | CheckpointLazy | `requiresAuth, requiresAdmin` | `CheckpointLazyView.vue` (тикет 135) |
| `/admin/checkpoints/list` | CheckpointEntityList | `requiresAuth, requiresAdmin` | `EntityListView.vue` (тикет 133) |
| `/admin/checkpoints/child/:id` | CheckpointChildCard | `requiresAuth, requiresAdmin` | `ChildCardView.vue` (тикет 133; с тикета 151 — только today-scoped история Checkpoint-сканов ребёнка, карточка ребёнка и «Bearbeiten» перенесены на `/child/:id`) |
| `/admin/checkpoints/betreuer/:id` | CheckpointBetreuerCard | `requiresAuth, requiresAdmin` | `BetreuerCardView.vue` (тикет 133) |
| `/admin/checkpoints/group-entity/:id` | CheckpointGroupEntity | `requiresAuth, requiresAdmin` | `GroupEntityView.vue` (тикет 133) |
| `/child/:id` | ChildDetail | `requiresAuth` | `ChildDetailView.vue` — с тикета 151 объединённая карточка ребёнка (данные + баннер присутствия + Bearbeiten/Entfernen), для `userStore.isAdmin` добавлена кнопка «Checkpoints anzeigen» → `/admin/checkpoints/child/:id` |
| `/child-edit/:id` | ChildDetailEdit | `requiresAuth` | `ChildEditView.vue` |
| `/armband-connect/:id` | ArmbandConnect | `requiresAuth` | `ArmbandConnectView.vue` |
| `/days-edit` | DaysEdit | `requiresAuth, requiresAdmin` | `DaysEditView.vue` (см. [[days]]) |
| `/config` | Config | `requiresAuth, requiresAdmin` | `ConfigView.vue` (см. [[config]]) |
| `/users-edit` | UsersEdit | `requiresAuth, requiresAdmin` | `UsersView.vue` (см. [[users]]) |
| `/invite` | InviteGenerator | `requiresAuth, requiresAdmin` | `InviteGeneratorView.vue` (см. [[Обзор-Edge-Functions]]) |
| `/reset-password` | ResetPassword | `requiresAuth` | `ResetPasswordView.vue` |

Не в списке маршрутов, но существуют как views: `DailyCheckInModalView.vue`,
`InviteAcceptView.vue`, `OfflineView.vue` — либо встроены как модалки (см.
[[Группы-и-рабочий-день]]), либо смонтированы отдельно от основного роутера.

## Публичные vs защищённые

- `meta.public: true` — доступны без регистрации вообще (гостевой доступ,
  см. [[Модель-аутентификации]]): `Login`, `Welcome`.
- `requiresAuth: false` без `public` — `Main`, `Info`: доступны и гостям, и
  авторизованным (не редиректят на `/login`), но не входят в «гостевой
  белый список» наравне с `public`.
- `requiresAuth: true` — обязательна активная сессия + активный пользователь
  в [[users]].
- `requiresAdmin: true` — дополнительно `role === 'admin'`.

## `?id=`/`?n=` → `/armband/:id`

Проверка `to.query.id || to.query.n` стоит в самом конце `router.beforeEach`,
**после** веток `meta.public` и `meta.requiresAuth` — а обе эти ветки
возвращаются через `next(...)` раньше. Значит редирект физически достижим
только для маршрутов без `public` и без `requiresAuth: true` — по факту в
текущей карте маршрутов это ровно `/main` и `/info` (оба `requiresAuth:
false`, без `public`). Не «любой маршрут» (уточнено по итогам ревью тикета
108, `tickets/108/REVIEW_REPORT.md`, Major 4) — но именно `/main` и есть
основной сценарий: короткая QR-ссылка ведёт на `/` → редирект на `/main` →
этот блок кода выполняется. Это и есть точка входа публичного QR-URL (см.
[[QR-код-URL]]) — само приложение открывается по короткой ссылке с
параметром, а не по прямому пути `/armband/:id`.

## Связанные заметки

- [[Модель-аутентификации]]
- [[QR-код-URL]]
- [[RLS-политики]]
- [[Checkpoint]]
