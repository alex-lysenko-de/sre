# Измененные файлы

| Файл | Изменение |
|---|---|
| `src/router/index.js` | Добавлен новый маршрут `/scanner-prototype` (`name: 'ScannerPrototype'`), рядом с существующей записью `/scanner`, по тому же образцу (`requiresAuth: true, requiresAdmin: false`, ленивая загрузка компонента). |
| `src/views/MainView.vue` | Добавлена новая кнопка меню "Scannen (Neu, Prototyp)" (видна при `isAuthenticated`, как и существующая кнопка "Scannen") и обработчик `goToScanPrototype()` → `router.push('/scanner-prototype')`. Существующая кнопка "Scannen"/`goToScan()` не изменена. |

# Новые файлы

| Файл | Назначение |
|---|---|
| `src/composables/useScannerFeedback.js` | Технические примитивы Web Audio/vibration (`getAudioContext`/`unlockAudioContext`, `playTone`, `vibrate`, `closeAudioContext`) без пресетов "успех"/"ошибка" — конкретные частоты/паттерны задаёт вызывающий код. |
| `src/components/scanner/Scanner.vue` | Переиспользуемый базовый слой прототипа: камера (инициализация, старт/стоп, выбор камеры, защита от параллельных переходов `isTransitioning`), сохранение выбранной камеры в `localStorage` (ключ `scanner_preferred_camera_id` — общий с `ScannerView.vue`, т.к. это предпочтение устройства), индикатор текущей камеры с кнопкой возврата к "Automatisch", раскладка "область сканирования вверху / управление внизу", индустриальная логика скана (пауза на распознавание, звук+вибрация независимо от исхода, экран подтверждения зелёный/красный ~0.8 с), резолв QR → браслет → ребёнок через `useArmband.getBraceletStatus()` (только чтение). Результат передаётся наверх через обязательный проп `onChildResolved(result)`. |
| `src/views/ScannerPrototypeView.vue` | Экран-прототип: монтирует `Scanner`, дедуплицирует сканы "в рамках текущего захода", собирает `PresencePacket`-подобный объект (`type: 'GROUP'`, `author_id`, `group_id`, `started_at`/`finished_at`, `children[]` с `{ child_id, timestamp, method: 'SCAN' }`), список отсканированных детей, кнопка "Senden" — заглушка (не выполняет сетевых вызовов), кнопка "Beenden" (останавливает сканер через `defineExpose({ stop })` и возвращает на `/main`). |

# Реализованные изменения

Реализовано строго по `IMPLEMENTATION_PLAN.md` (план "2 слоя", зафиксированный в `DECISIONS.md` по итогам ревью архитектуры):

1. **`ScannerView.vue` (боевой `/scanner`) не тронут ни в одной строке** — подтверждено `git status --short src/` (файл не входит в список изменённых/новых).

2. **`useScannerFeedback.js`** — написан заново (не перенесён из `ScannerView.vue`), содержит только технические примитивы (`AudioContext`, `playTone`, `navigator.vibrate`), без знания о доменных исходах "успех"/"ошибка".

3. **`Scanner.vue`** — покрывает пункты 1-3, 7 тикета 116.txt целиком и базовую часть пунктов 4-6:
   - Пункт 1: выбранная камера сохраняется в `localStorage` и используется при следующей инициализации (`initScanner()`, аналогично механике `ScannerView.vue`, но реализовано независимо).
   - Пункт 2: индикатор `currentCameraLabel` вместо статус-бейджа "Scanner aktiv"; кнопка "Automatisch (Rückkamera)" доступна для возврата в любой момент (не блокируется после ручного выбора камеры, как было в старом сканере) — при нажатии дополнительно очищает сохранённое предпочтение в `localStorage`, чтобы следующий запуск тоже стартовал автоматически.
   - Пункт 3: фронтальные камеры видны в общем списке (`Html5Qrcode.getCameras()` не фильтрует по типу — как и в 103); раскладка — область сканирования (`.proto-scan-area`, `flex: 1 1 auto`) вверху, ничего не отображается над ней.
   - Пункт 7: все элементы управления (индикатор камеры, кнопки выбора/автоматического режима, выпадающий список камер) — в нижнем блоке `.proto-scanner-controls`.
   - Пункты 4-6 (базовая часть): при каждом успешном сыром декоде — `pause(true)`, нейтральный сигнал "scan" + короткая вибрация, независимо от дальнейшей бизнес-логики (реализует явное требование 116.txt: "звук всегда при успешном считывании, вне зависимости от бизнес-логики"). Далее резолв (невалидный формат / браслет не найден / ребёнок найден) — свой сигнал и вибрация для каждого исхода (таблица сигналов плана), полноэкранный (в рамках области сканирования) цветной экран подтверждения (~0.8 с, в диапазоне 0.5-1 с из требования), после чего `resume()`.
   - Результат резолва передаётся наверх через `onChildResolved(result)` (обязательный проп); ожидаемый `{ title, subtitle, variant? }` используется для текста/цвета экрана подтверждения — по умолчанию (`defaultDisplayFor`) для `invalid`/`not-found`/`found`, с возможностью переопределения слоем 2.
   - `recordChildPresence()` не импортируется и не вызывается нигде в файле.

4. **`ScannerPrototypeView.vue`** — покрывает верхний уровень:
   - Дедупликация "в рамках захода": если `child.id` уже есть в собранном пакете — воспроизводится дополнительный сигнал "duplicate" + короткая вибрация (поверх уже воспроизведённого `Scanner.vue` сигнала "success", как и предписано планом: "слой 2 может добавить дополнительный сигнал... не вместо"), возвращается `{ title: 'Bereits gescannt', subtitle: <имя>, variant: 'error' }`, элемент не добавляется в пакет повторно.
   - Сборка пакета: `type: 'GROUP'`, `author_id: userStore.userInfo.id`, `group_id: userStore.userInfo.group_id`, `started_at` фиксируется при первом успешном не-дублирующемся резолве, `finished_at` обновляется на каждый скан (и дополнительно фиксируется при нажатии "Senden").
   - "Senden" — заглушка: только устанавливает `finished_at` и локальное сообщение `sentMessage`, не выполняет `fetch`/Supabase-вызовов.
   - "Beenden" — останавливает сканер (`scannerRef.value.stop()`) и возвращает на `/main`.

5. **Роутер + меню** — новый маршрут `/scanner-prototype` рядом с `/scanner`; новая кнопка в `MainView.vue` с подписью "Scannen (Neu, Prototyp)", явно отличающей её от существующей кнопки "Scannen", видна при том же условии (`isAuthenticated`), не заменяет и не скрывает существующую кнопку.

Иконки Font Awesome: для экрана подтверждения и индикатора камеры использованы только уже зарегистрированные в `main.js` иконки (`faCheckCircle`, `faTimes`) — `main.js` не менялся, чтобы не расширять список зависимостей сверх необходимого (иконка "times" вместо "times-circle" для красного экрана, простой текст без иконки для индикатора камеры).

# Отклонения от плана

Нет содержательных отклонений от `IMPLEMENTATION_PLAN.md`. Одна мелкая техническая деталь, не затронутая планом явно: чтобы не добавлять новые Font Awesome иконки в `main.js` (`faTimesCircle`, `faCamera`), использованы уже зарегистрированные `faTimes`/без иконки для индикатора камеры — визуально равнозначно, архитектурно не значимо.

# Миграции

Нет. Прототип не обращается к БД вообще (кроме `useArmband.getBraceletStatus()` — существующий read-only метод, сигнатура не менялась). Изменений схемы, RLS, Edge Functions нет.

# Проверки

- `git status --short src/` — подтверждён ожидаемый набор изменений: `src/router/index.js`, `src/views/MainView.vue` изменены; `src/composables/useScannerFeedback.js`, `src/components/scanner/Scanner.vue`, `src/views/ScannerPrototypeView.vue` — новые файлы; `src/views/ScannerView.vue` в списке отсутствует (не изменён).
- `npm run build` — сборка проходит успешно (327 модулей, без ошибок; появились два новых чанка `ScannerPrototypeView-*`; предупреждения о версии Node/размере чанков/возрасте browserslist — существовавшие ранее, не связаны с этой правкой).
- Статический просмотр кода `Scanner.vue`/`ScannerPrototypeView.vue` подтверждает: `useArmband.recordChildPresence` не импортирован ни в одном из новых файлов; кнопка "Senden" не содержит вызовов `fetch`/`supabase`.
- **Ручное тестирование на реальных Android/iPhone-устройствах (требование Definition of Done плана, п. 6 «План реализации») не выполнено в рамках этой сессии** — окружение без доступа к реальным устройствам с камерой. Не проверено интерактивно:
  - сохранение выбранной камеры между визитами и корректный возврат к "Automatisch";
  - сценарий фронтальной камеры (ребёнок сканирует сам, совпадение области сканирования и объектива);
  - звук/вибрация/экран подтверждения для всех исходов (успех/неверный QR/не найден/повторный скан) на реальном устройстве, включая известное ограничение iOS Safari (mute switch — см. риски плана);
  - поведение `pause(true)`/`resume()` из `html5-qrcode` на реальных устройствах (риск, явно отмеченный в плане как непроверенный на этапе архитектуры).

  Это единственный пункт Definition of Done, не закрытый в этой сессии. Рекомендуется выполнить ручную проверку по чек-листу плана (раздел «План реализации», п. 5) перед тем, как использовать прототип для реальной UX-оценки, на которой основано решение о дальнейшей судьбе тикета 120.

# Исправления после ревью

Устранены все находки `REVIEW_REPORT.md` (Major 1 и весь «Список обязательных
исправлений», п. 1-6), а также оба пункта «Список необязательных улучшений».
Затронуты только `src/components/scanner/Scanner.vue` и
`src/views/ScannerPrototypeView.vue` — `ScannerView.vue`, роутер и
`MainView.vue` в этом раунде не менялись.

1. **Major — сетевая/серверная ошибка резолва браслета маскировалась под
   "не найдено".** В `Scanner.vue` добавлен третий исход `{ status: 'error' }`
   (наряду с `invalid`/`not-found`/`found`): при исключении из
   `getBraceletStatus()` теперь показывается "Fehler beim Prüfen des
   Armbands / Bitte erneut versuchen" со своим сигналом (`SOUND.error`), а не
   "Kein Kind mit dem Verband verbunden".
2. **Квадратная область сканера.** `.proto-scan-area` переведена с
   `flex: 1 1 auto` (растягивание на всю оставшуюся высоту) на
   `aspect-ratio: 1 / 1` (высота = ширина экрана); добавлены `:deep()`-правила
   для `video`/`canvas`, которые `html5-qrcode` вставляет динамически
   (`object-fit: cover`, `width/height: 100% !important`), чтобы под видео не
   оставалось чёрного поля. Требует, как и отмечено в самом ревью, проверки
   на нескольких реальных устройствах — см. «Проверки» ниже.
3. **Единая кнопка выбора камеры.** Индикатор + две кнопки + отдельный
   `<select>` с плейсхолдером заменены на одну кнопку с текущим значением
   (`Kamera: {{ currentCameraLabel }}`), открывающую прокручиваемый список
   (`.proto-camera-list`), где "Automatische Kamerawahl" — обычный, ничем не
   выделенный пункт списка наравне с камерами устройства. Функции
   `selectCamera`/`useAutomaticCamera` объединены в одну `chooseCamera(deviceId
   | null)`. Промежуточное состояние "камера не выбрана"/disabled-плейсхолдер
   убрано полностью — заодно закрывает Minor-находку про пустой `<select>`.
4. **Длительность экрана подтверждения по исходу.** Константа `wait(800)`
   заменена на `CONFIRMATION_DURATION_MS = { success: 1600, error: 3000 }`,
   выбор длительности — по итоговому `variant` экрана (не по исходной причине
   скана), используется как в основном потоке скана, так и в новом
   `showExternalMessage()` (см. п. 6).
5. **Повторный скан "в рамках захода" — не ошибка.** В `ScannerPrototypeView.vue`
   при обнаружении уже отсканированного ребёнка возвращается
   `{ variant: 'success', repeat: true }` вместо `{ variant: 'error' }`;
   звук — стандартный "success" (уже воспроизводится в `Scanner.vue` для
   любого исхода `found`, включая повтор, отдельный сигнал "duplicate" в
   прототипе убран как избыточный). Визуальная индикация повтора — второй
   значок `check-circle` (`confirmation.repeat`, `.proto-confirmation-icon-repeat`)
   рядом с основным. В пакет повторно не добавляется — логика дедупликации не
   менялась.
6. **Реакция на "Senden".** `Scanner.vue` получил новый экспортируемый метод
   `showMessage(variant, display, durationMs)` — переиспользует тот же
   полноэкранный компонент подтверждения для внешних сообщений (пауза скана
   на время показа, если сканер активен). `ScannerPrototypeView.vue` вызывает
   его в `sendStub()` (зелёный экран "Gesendet" при успехе — единственный
   реалистичный путь для заглушки; красная ветка на случай ошибки оставлена
   на уровне кода, хотя у самой заглушки нет реального источника отказа) и
   затем полностью сбрасывает раунд (`resetRound()`: `packet.children`,
   `scannedList`, `started_at`/`finished_at` — через `splice`, реактивность
   сохраняется). Прежнее мелкое `alert alert-info` с текстом удалено вместе с
   `sentMessage`. Вертикальный `padding` увеличен для "Beenden"/"Senden"
   (`.proto-summary-actions .btn`, `16px 18px`) и для кнопки выбора камеры
   (`.proto-camera-picker-btn`, `14px 18px`).

Также выполнены оба необязательных улучшения: неиспользуемый
`ref="qrReaderEl"` удалён из `Scanner.vue` (поиск камеры по-прежнему идёт по
строковому `id="qr-reader-proto"`); нулевое `packet.group_id` явно
задокументировано комментарием над определением `packet` в
`ScannerPrototypeView.vue` как известное упрощение, не подлежащее
бездумному копированию в 120.

# Проверки после ревью

- `git status --short src/ tickets/116/` — изменены ровно два файла:
  `src/components/scanner/Scanner.vue`, `src/views/ScannerPrototypeView.vue`
  (плюс служебные файлы тикета). `ScannerView.vue`, роутер, `MainView.vue`,
  `main.js` не тронуты.
- `npm run build` — сборка проходит успешно (327 модулей, без ошибок;
  предупреждения о версии Node/размере чанков/возрасте browserslist —
  существовавшие ранее, не связаны с этой правкой).
- Статический просмотр: сообщение "Kein Kind mit dem Verband verbunden"
  теперь недостижимо из `catch`-ветки `getBraceletStatus()` — только из
  явного `!child` после успешного запроса.
- **Повторная ручная проверка на реальных устройствах не выполнена** — как
  и в исходной реализации, окружение без доступа к устройствам с камерой.
  В частности, не подтверждено на практике: устраняет ли `aspect-ratio` +
  `object-fit: cover` чёрное поле под видео на том же Android-устройстве,
  где было впервые замечено (ревью отдельно указывает, что первопричина —
  устройство-специфичная или общая — не установлена), и корректно ли
  показывается прокручиваемый список камер на iPhone с несколькими
  объективами. Рекомендуется закрыть перед следующим раундом ревью.
