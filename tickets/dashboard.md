# Дашборд тикетов

Обзорная доска, показывающая порядок выполнения тикетов. После создания нового тикета или смены его статуса — обновлять эту таблицу.

| № | Название | Статус |
|---|----------|--------|
| [101](101/101.txt) | Config: ошибка доступа "keine Rechte" для Hauptbetreuer | DONE |
| [102](102/102.txt) | Days-edit: удаление дня не сохраняется в БД (появляется снова после reload) | DONE |
| [103](103/103.txt) | Scanner: выбор камеры на iPhone (селфи вместо основной) + громкость/вибрация/визуальный фидбек | DONE |
| [104](104/104.txt) | AdminBusView (/admin-busses): упрощение интерфейса, отступы, шрифты | DONE |
| [105](105/105.txt) | Миграция хранения авторизации с Local Storage на LocalForage | CHANGES_REQUIRED |
| [106](106/106.txt) | Kopfzählung (Headcount) для "Meine Gruppe" | FIXES_APPLIED |
| [107](107/107.md) | Ревизия и уборка файлов проекта / документации | AUDIT_DONE |
| [108](108/108.md) | База знаний проекта (Obsidian vault) | DONE |
| [109](109/109.md) | Пользовательская документация | не начат |
| [110](110/110.md) | Миграция в сторону multi-tenant (план) | не начат |
| [111](111 — DB backup tooling, коммит 17e206d, без файла в tickets/) | Backup-тулинг для БД (pg_dump schema+data) | DONE (только в git-истории) |
| [112](112/112.txt) | Сохранение авторизации в PWA на iPhone (Safari / Add to Home Screen) | DEVELOPMENT_DONE |
| [114](114/114.txt) | Bug fix: редирект на /info вместо home page после логина/регистрации | DONE |
| [115](115/115.txt) | Bug fix: работа с группой "null" | DONE |
| [113](113/113.txt) | Автоматическое создание PWA-приложения | CHANGES_REQUIRED |
| [117](117/117.txt) | LocalForage ↔ Supabase: архитектура обмена данными | тикет создан |
| [116](116/116.txt) | UI-прототип нового сканера (для будущей функциональности тикета 120; существующий `/scanner` не меняется). Решения зафиксированы в [tickets/116/DECISIONS.md](116/DECISIONS.md). Реализовано по [tickets/116/IMPLEMENTATION_PLAN.md](116/IMPLEMENTATION_PLAN.md) — 2 слоя (`Scanner.vue` + `ScannerPrototypeView.vue`), новый маршрут `/scanner-prototype`, кнопка "Отправить" — заглушка. По итогам ревью и ручного тестирования на устройствах исправлены: различение ошибки сети vs "браслет не найден", квадратная область сканера, единая кнопка выбора камеры, длительность экрана подтверждения по исходу, повтор скана = успех (не ошибка), сброс раунда + результат "Senden" в области сканера (см. `IMPLEMENTATION_REPORT.md`). Повторное ревью — PASS (`REVIEW_REPORT.md`); визуальную повторную проверку на реальных устройствах (Samsung/iPhone) рекомендуется выполнить перед UX-решением по 120 | DONE |
| [118](118/118.txt) | Рефакторинг документации: уборка doc/ после базы знаний + компактная сводка для ИИ (CLAUDE.md). Backlog нереализованных идей AdminBusView, спасённый из удалённого `doc/AdminBusView_TechTask.md`: [tickets/118/AdminBusView_Backlog.md](118/AdminBusView_Backlog.md) | FIXES_APPLIED |
| [120](120/120.txt) | Модуль перекличек: 3 подрежима сканирования (автобус/группа/свободные отметки) поверх базового сканера, пакетная отправка результатов (PresencePacket). `120.txt` переработан в единый актуальный источник требований (архив: `DECISIONS.md`, старый `implementation_plan.md`). Реализовано по [tickets/120/IMPLEMENTATION_PLAN.md](120/IMPLEMENTATION_PLAN.md) — новый `useScanPacket.js` (сборка/отправка пакета) + три View (`ScannerBusView`/`ScannerGroupView`/`ScannerCheckinView`), переиспользующие `Scanner.vue` из 116 без изменений; маршруты `/scanner/bus|group|checkin`; `ScannerView.vue`/`ScannerPrototypeView.vue` и `useArmband.recordChildPresence()` удалены. По итогам ревью (`REVIEW_REPORT.md`, CHANGES_REQUIRED) исправлено: `finished_at` больше не пересчитывается при повторной отправке, ошибки отправки/загрузки ростера показывают реальный текст ошибки и кнопку повтора; риск рассинхронизации звук/вибрация vs визуальный результат при скане "чужой" группы в GROUP — осознанно принят и задокументирован (контракт `Scanner.vue` не меняется в рамках 120). Сквозная проверка на реальных устройствах против задеплоенной `submit-scan-packet` по-прежнему отложена до совместного релиза с 122 (см. `IMPLEMENTATION_REPORT.md`) | BUGFIX_DONE |
| [121](121/121.txt) | Research: сравнение архитектурных вариантов серверной обработки пакетов сканирования (подготовка к 122). Результат: [tickets/121/IMPLEMENTATION_PLAN.md](121/IMPLEMENTATION_PLAN.md) — вариант A (заголовок пакета + переиспользование `scans`) с обязательной пакетной переработкой триггеров (устранение N-кратных каскадов), Edge Function `submit-scan-packet`, идемпотентность по `client_packet_id`, схема заранее совместима с будущей отменой пакета | ARCHITECT_DONE |
| [122](122/122.txt) | Серверная логика обработки пакетов сканирования (приём/хранение `scan_packets`, идемпотентность, батч-триггеры, UI администратора) — реализуется одновременно с 120, на основе research-а 121. Реализовано по [tickets/122/IMPLEMENTATION_PLAN.md](122/IMPLEMENTATION_PLAN.md): SQL в [doc/db/scan_packets.sql](../doc/db/scan_packets.sql) (таблица `scan_packets`, `scans.packet_id`/`method`, батч-триггеры на transition tables для `on_scan_insert`/`on_children_today_change`, функция `submit_scan_packet()`, RLS), Edge Function `supabase/functions/submit-scan-packet`, композабл `useScanPackets.js` (админ-чтение), пакеты видны в `BusDetailModal.vue` и новом `GroupDetailModal.vue` (вход из `ChildrenView.vue`). По итогам ревью (`REVIEW_REPORT.md`) исправлено: батч-триггер и `submit_scan_packet()` теперь явно `RAISE EXCEPTION` на невозможных состояниях данных («ребёнок без группы», «элемент `children[]` без `child_id`») вместо тихого молчания — по решению автора задачи такие случаи не «обрабатываются», а считаются нарушением инварианта. Баг «Fehler beim Senden, failed to fetch» диагностирован и устранён: SQL уже был применён к БД, но Edge Function ни разу не была задеплоена (`404 NOT_FOUND`, из-за чего браузер видел провал CORS-preflight как `Failed to fetch`) — задеплоена (`npx supabase functions deploy submit-scan-packet`), проверена (`OPTIONS`→200). Регрессия по `HeadcountView`/`ChildrenView`/`AdminBusView`/`ChildDetailView` и сквозная проверка с 120 на устройствах — по-прежнему не выполнены (см. `IMPLEMENTATION_REPORT.md`) | BUGFIX_DONE |
| [123](123/123.txt) | ~~Редизайн интерфейса Kopfzählung~~ — фича признана неудачным экспериментом и быть удалена
| [124](124/124.txt) | Клиентское логирование ошибок + версионность приложения. План: [tickets/124/IMPLEMENTATION_PLAN.md](124/IMPLEMENTATION_PLAN.md) — модуль `clientLog.js` (ring-buffer breadcrumbs, перехват `errorHandler`/`window.onerror`/`unhandledrejection`, патч `console.error`/`warn` вместо правки composables, Pinia-плагин для breadcrumbs действий), таблица `client_logs` (BEFORE INSERT триггер проставляет `user_id` из `auth.uid()`, RLS — только INSERT, без SELECT из приложения), версия из `package.json` через `vite.config.js` `define`, новый экран `DiagnosticsView.vue` (версия + локальный лог + Web Share/копирование). Toast/push/чат — намеренно не входят, отдельная схема не создаётся под них. Ревью ([REVIEW_REPORT.md](124/REVIEW_REPORT.md)) — реализация фактически отсутствует (нет `IMPLEMENTATION_REPORT.md`, ноль изменений в затронутых планом файлах) | CHANGES_REQUIRED |
| [126](126/126.txt) | UI-доработки навигации и сканера: | BUGFIX_DONE |
| [130](130/130.txt) | Редизайн интерфейса администратора для переклички — дизайн-этап (только концепция, БД/код не менялись). Сущность «Перекличка» переименована в «Контрольная точка» (Checkpoint), выбрана гибридная модель (авто-создание по первому пакету, explicit-only Finish, независимые типы). Домен и терминология (Roster/ScanPacket/Aggregation Model и т.д.) переработаны и перенесены в постоянную документацию — [vault/02-Предметная-область/Checkpoint.md](../vault/02-Предметная-область/Checkpoint.md). Ticket-папка готовится к удалению — ссылки на неё из vault убраны | ARCHITECT_DONE, содержание перенесено в vault |
| [130_2](130_2/130_2.txt) | UI-прототип экрана управления контрольными точками на mock-данных (без БД/сети), прошёл 4 раунда UX-фидбека, принят пользователем. Изолированные маршруты `/admin/checkpoints-prototype/*`, entity-центрированный редизайн (Child/Betreuer/Group как отдельные сущности со своей карточкой) | DEVELOPMENT_DONE |
| [131](131/131.txt) | Архитектурный план реализации Checkpoint (БД+клиент) поверх 130/decision.md и принятого прототипа 130_2 — [tickets/131/IMPLEMENTATION_PLAN.md](131/IMPLEMENTATION_PLAN.md). Реализация разбита на последовательность тикетов 132–139, см. сводный план [tickets/131/MIGRATION_PLAN.md](131/MIGRATION_PLAN.md) (ревизия 2, по итогам независимого архитектурного ревью — [migration_Plan_review.md](131/migration_Plan_review.md), разбор замечаний в [MIGRATION_PLAN_REVIEW_RESPONSE.md](131/MIGRATION_PLAN_REVIEW_RESPONSE.md); подтверждено пользователем: живых пользователей/данных нет, деление Phase 1/Gate/Phase 2 из ревизии 1 убрано, 13 тикетов → 8) | ARCHITECT_DONE, реализация вынесена в 132–139 |
| [132](132/132.txt) | DB: полная схема Checkpoint — таблица `checkpoints` + RLS + RPC `create/finish/reopen/remove_checkpoint` + расширение `submit_scan_packet()`. Реализовано по [tickets/132/IMPLEMENTATION_PLAN.md](132/IMPLEMENTATION_PLAN.md) — [doc/db/checkpoints.sql](../doc/db/checkpoints.sql) подтверждённо применён к боевой БД (виден в свежем дампе `backup/database/schema.sql`, побайтово совпадает); ручная проверка на устройстве — всё ещё отдельный шаг (см. [IMPLEMENTATION_REPORT.md](132/IMPLEMENTATION_REPORT.md)) | DEVELOPMENT_DONE |
| [133](133/133.txt) | Composable-слой Checkpoint + Entity-слой на реальных данных (Child/Betreuer/Group карточки, универсальный список). Реализовано по [tickets/133/IMPLEMENTATION_PLAN.md](133/IMPLEMENTATION_PLAN.md) — `useSupabaseCheckpoints.js`/`useCheckpoints.js`/`useLazyCheckpointProgress.js`/`useBetreuerEntity.js` + 4 новых экрана/5 компонентов, `npm run build` проходит. Применение 132 к боевой БД и ручная проверка — ещё не выполнены (см. [IMPLEMENTATION_REPORT.md](133/IMPLEMENTATION_REPORT.md)) | DEVELOPMENT_DONE |
| [134](134/134.txt) | Экраны Checkpoint (реальные): List + Bus + Group. Реализовано по [tickets/134/IMPLEMENTATION_PLAN.md](134/IMPLEMENTATION_PLAN.md) — `CheckpointListView.vue`/`CheckpointBusView.vue`/`CheckpointGroupView.vue` + 4 компонента (`CheckpointCreateModal`/`StatusBadge`/`TypeBadge`/`OriginBadge`) на `useCheckpoints.js` (133), маршруты `/admin/checkpoints`, `.../bus/:id`, `.../group/:id`; Realtime на каждом экране слушает `checkpoints`+`scan_packets` напрямую (composable-подписка 133 покрывает только `checkpoints`). [REVIEW_REPORT.md](134/REVIEW_REPORT.MD) — Approved with comments; Major (бейдж происхождения отсутствовал в списке) и Minor (клик по Lazy-карточке вёл на несуществующий маршрут) исправлены, см. [IMPLEMENTATION_REPORT.md](134/IMPLEMENTATION_REPORT.md) «Правки по REVIEW_REPORT.md»; Suggestion (сворачивание «чистых» групп) оставлен сознательно — унаследован из уже одобренного прототипа 130_2. `npm run build` проходит. Ручная проверка на устройстве — не выполнена | BUGFIX_DONE |
| [135](135/135.txt) | Экран Checkpoint (реальный): Lazy. Реализовано по [tickets/135/IMPLEMENTATION_PLAN.md](135/IMPLEMENTATION_PLAN.md) — `CheckpointLazyView.vue` на `useCheckpoints.js`/`useLazyCheckpointProgress.js` (133), маршрут `/admin/checkpoints/lazy/:id`; тот же Realtime/Finish-Reopen-Remove-паттерн, что 134. Заодно снята временная `alert()`-заглушка клика по Lazy-карточке в `CheckpointListView.vue` (134, Minor-фикс ревью) — маршрут теперь существует. `npm run build` проходит. Ручная проверка на устройстве — не выполнена | DEVELOPMENT_DONE |
| [136](136/136.txt) | `ChildDetailView.vue`: «Präsenz registrieren» через `submit_scan_packet()` (устранение обхода Packet-модели, находка независимого ревью). Реализовано по [tickets/136/IMPLEMENTATION_PLAN.md](136/IMPLEMENTATION_PLAN.md) — `handlePresenceConfirm()` теперь `createPacket('CHECKIN')` → `addManual()` → `submitPacket()` вместо прямого `useScan.createScan()`. Известное следствие: выбор автобуса в `ChildPresenceModal.vue` больше не сохраняется (CHECKIN-пакеты не несут `bus_id`, констрейнт БД) — модалка не менялась, вне охвата 136. `npm run build` проходит. Ручная проверка на устройстве — не выполнена | DEVELOPMENT_DONE |
| [137](137/137.txt) | Удаление устаревшего кода + объединение меню администратора. Реализовано по [tickets/137/IMPLEMENTATION_PLAN.md](137/IMPLEMENTATION_PLAN.md) — 28 файлов удалено (`AdminBusView.vue`/`ChildrenView.vue`/`HeadcountView.vue`+`useChildPresence.js`/`useGroups.js`/`useBusData.js`/`ResetHistoryPanel.vue`/`Bus`+`GroupDetailModal.vue`/весь prototype-слой), `useDays.js` очищен от Reset-мертвяка, `router/index.js`/`MainView.vue` (одна кнопка «Checkpoints»)/`GroupEditView.vue` обновлены, новый [doc/db/remove_reset_events_trigger.sql](../doc/db/remove_reset_events_trigger.sql) (не применён). Находки сверх списка тикета (грепом): осиротевшие `useScanPackets.js`/`useGroupScanSession.js` тоже удалены, мёртвая кнопка «Kopfzählung» в `MainView.vue` и мёртвая ссылка `/children` в `App.vue` (не входил в «Затрагиваемые части») тоже исправлены — см. [IMPLEMENTATION_REPORT.md](137/IMPLEMENTATION_REPORT.md). `npm run build` проходит, бандл заметно меньше (94→59 precache записей). Применение SQL-миграции и ручная проверка на устройстве — не выполнены | DEVELOPMENT_DONE |
| [138](138/138.txt) | DB: date-scoped `children_today`/`groups_today` (замена Hard/Soft/Total Reset) + чистка неотозванной legacy RLS-политики. Реализовано по [tickets/138/IMPLEMENTATION_PLAN.md](138/IMPLEMENTATION_PLAN.md) — новый [doc/db/date_scoped_daily_tables.sql](../doc/db/date_scoped_daily_tables.sql): колонка `date` + unique-констрейнты `(child_id, date)`/`(group_id, date)`, атомарно обновлённые `ON CONFLICT`-цели в `on_scan_insert_batch()`/`on_children_today_change_batch()`, обновлённая (не удалённая — живой ручной инструмент восстановления) `recalculate_groups_today()`, отозваны все широкие legacy RLS-политики на обеих таблицах + узкие `own_group_*` (потребитель удалён тикетом 137, подтверждено грепом) — итоговая модель как у `checkpoints`. `vault/03-База-данных/RLS-политики.md` обновлён. `src/` не тронут (нет читателей/писателей после 137). **Подтверждено применённой к боевой БД** (сессия тикета 139: коммит `1733ddc` содержит обновлённый `backup/database/schema.sql`, побайтово совпадающий с миграцией). Ручная проверка реального скана на устройстве — не выполнена | DEVELOPMENT_DONE |
| [139](139/139.txt) | Финальная сверка документации `vault/`/`CLAUDE.md` с фактическим состоянием кода после 132-138 + подготовка чек-листа сквозной проверки на устройстве. Реализовано по [tickets/139/IMPLEMENTATION_PLAN.md](139/IMPLEMENTATION_PLAN.md): новые страницы [vault/02-Предметная-область/Checkpoint.md](../vault/02-Предметная-область/Checkpoint.md) (доменная модель) и [vault/03-База-данных/checkpoints.md](../vault/03-База-данных/checkpoints.md) (таблица+RPC); помечены историческими (не удалены) `Переклички.md`/`Учёт-автобусов.md`/`reset_events.md`/`useBusData.md`/`useChildPresence.md`/`useGroups.md`/`useDays.md` (раздел Reset-операций); обновлены `children_today.md`/`groups_today.md` (date-scoped DDL, ошибочный `groups_today_user_id_key` в старой версии заметки исправлен), `Карта-маршрутов.md` (актуальные `/admin/checkpoints/*`/`/scanner/*`, убраны мёртвые `/admin-busses`/`/children`/`/headcount`), `Обзор-composables.md`, `useChildren.md` (текущие потребители), `MOC.md`; `CLAUDE.md` дополнен фактом об автоматической границе дня. Попутно подтверждено применение 138 к боевой БД (см. выше). По итогам ревью (`REVIEW_REPORT.MD`, Changes requested) исправлен пропущенный Major: `vault/03-База-данных/Триггеры.md` переписан под живые `on_scan_insert_batch()`/`on_children_today_change_batch()` (тикет 122) + date-scoped таргеты (тикет 138), `on_reset_event_insert` помечена исторической (функция физически удалена тикетом 137); поправлена утёкшая неточность в таблице «Поля присутствия» `children_today.md`. См. [IMPLEMENTATION_REPORT.md](139/IMPLEMENTATION_REPORT.md), «Правки по REVIEW_REPORT.MD». **Сквозная ручная проверка на устройстве — по-прежнему не выполнена** (нет браузера/устройства в сессии ИИ-ассистента), чек-лист сценариев — [IMPLEMENTATION_PLAN.md](139/IMPLEMENTATION_PLAN.md), «Чек-лист» — ждёт пользователя | BUGFIX_DONE (документация); устройство — не начато |
| [140](140/140.txt) | Небольшие правки в интерфейсе: точечная локализация оставшихся RU/EN строк (`Scanner.vue`/`ScannerBusView.vue`/`ScannerCheckinView.vue`/`ScannerGroupView.vue`/aria-label в `ResetPasswordView.vue`/`UsersView.vue`); `ArmbandView.vue` — ID браслета в заголовке + напротив каждого ребёнка в списке "Kind auswählen"; переработка виджета Checkpoint — `CheckpointOriginBadge.vue`/`CheckpointStatusBadge.vue` больше не бейджи/квадраты (обычный текст "Ersteller:"/"Status:", создатель всегда по имени, не "Admin"), `cp-item-result`/`cp-result-row` перемещён в самый низ карточки и увеличен, заметность не теряется для закрытых checkpoint'ов — правка применена и в списке (`CheckpointListView.vue`), и во всех трёх детальных вью (Bus/Group/Lazy). См. [IMPLEMENTATION_PLAN.md](140/IMPLEMENTATION_PLAN.md)/[IMPLEMENTATION_REPORT.md](140/IMPLEMENTATION_REPORT.md). `npm run build` проходит. Ручная проверка на устройстве — не выполнена. Ревью: [REVIEW_REPORT.md](140/REVIEW_REPORT.md) — PASS (Minor: возможный CSS-каскад приглушает цвет Ersteller/Status в закрытых карточках списка; более поздний коммит `7f683de` переместил ID браслета из заголовка `ArmbandView.vue` в абзац ниже, само требование по-прежнему выполняется) | DONE |
| [141](141/141.txt) | Bug fix: после «Senden» в режимах сканирования (Bus/Group/Checkin) камера продолжает показывать картинку, но новые сканы не декодируются. Архитектурный анализ ([IMPLEMENTATION_PLAN.md](141/IMPLEMENTATION_PLAN.md)) вскрыл причину чтением исходников `html5-qrcode`: `resume()` после паузы зависит от нативного события `<video>` `"playing"`, которое может не сработать при пересекающихся `pause()`/`resume()`-циклах (обычный скан vs. `showExternalMessage()` при Senden) — сбой не бросает исключение и не отклоняет промис, FSM необратимо остаётся в `PAUSED`, декод выключен, хотя `foreverScan()` продолжает рисовать кадры. Реализовано по [IMPLEMENTATION_PLAN.md](141/IMPLEMENTATION_PLAN.md): в `Scanner.vue`/`showExternalMessage()` финальный `resume()` заменён на гарантированный `stopScanning()`+`startCamera()` (тот же путь инициализации, что и при открытии экрана), добавлен поясняющий комментарий; правка локальна, три `ScannerXxxView.vue`/`useScanPacket.js` не менялись. `npm run build` проходит. См. [IMPLEMENTATION_REPORT.md](141/IMPLEMENTATION_REPORT.md). Ручная проверка на устройстве (обязательна для подтверждения гипотезы) — не выполнена. Ревью: [REVIEW_REPORT.md](141/REVIEW_REPORT.md) — CHANGES_REQUIRED (Major: гонка между обычным циклом скана и `showExternalMessage()` — сценарий, который сам план называет наиболее вероятным триггером исходного бага — не устранена, только смягчена для одной стороны; данные не теряются, но возможны UI-глюки/неожиданный рестарт камеры посреди скана; Minor: новый `stop()+start()` не защищён от размонтирования компонента, `camera-started`/`camera-stopped` теперь эмитятся и как деталь показа сообщения) | CHANGES_REQUIRED |
| [142](142/142.txt) | Упростить сценарий просмотра/привязки ребёнка: (1) убрать предложение отметить присутствие сразу после привязки Armband — присутствие только через перекличку; (2) на `/group-edit` применить концепт «список виджетов» к списку детей. Реализовано по [IMPLEMENTATION_PLAN.md](142/IMPLEMENTATION_PLAN.md): в `ChildDetailView.vue` убраны кнопка «Präsenz registrieren», `handlePresenceConfirm()`/`openPresenceModal()`, `<ChildPresenceModal>` и связанные `useUserStore`/`useScanPacket`-импорты (стали неиспользуемыми), информационный баннер присутствия оставлен как факт-статус; добавлена кнопка «Entfernen» после «Bearbeiten» (`useChildren().deleteChild()` + `confirm()`, переход на `GroupEdit`); `ChildPresenceModal.vue` удалён как мёртвый код (единственный потребитель ушёл). В `GroupEditView.vue` кнопки «Bearbeiten»/«Entfernen» убраны из строки списка, вся строка кликабельна и ведёт на `/child/:id` (`openChildDetail()`), `editChild()`/`removeChild()` и ставший неиспользуемым `deleteChild` из `useChildren()` удалены. БД/API не менялись. См. [IMPLEMENTATION_REPORT.md](142/IMPLEMENTATION_REPORT.md). `npm run build` проходит. Ручная проверка в браузере — не выполнена | DEVELOPMENT_DONE |
| [143](143/143.txt) | Bug fix: экраны переклички (`CheckpointListView`/`CheckpointBusView`/`CheckpointGroupView`/`CheckpointLazyView`) не обновлялись в реальном времени при новых сканах с других устройств — помогала только ручная перезагрузка страницы. Реализовано по [IMPLEMENTATION_PLAN.md](143/IMPLEMENTATION_PLAN.md): подтверждена основная гипотеза плана статическим анализом дампа (`backup/database/schema.sql`) — таблицы `checkpoints`/`scan_packets` не входят в публикацию `supabase_realtime` (перечислены только `children`/`children_today`/`groups_today`/`reset_events`/`user_group_day`/`users`), из-за чего Postgres физически не публикует их WAL-изменения в логическую репликацию независимо от корректности клиентского кода/RLS; создан новый [doc/db/checkpoints_realtime_publication.sql](../doc/db/checkpoints_realtime_publication.sql) (`ALTER PUBLICATION supabase_realtime ADD TABLE checkpoints, scan_packets`) — **не применён к боевой БД** (нет инструмента прямого доступа к БД в сессии ИИ-ассистента, тот же паттерн, что и предыдущие SQL-миграции проекта). Мёртвый код `subscribeToCheckpointsChanges()` подтверждён (грепом по всему репозиторию — вызовов кроме экспорта не найдено) и удалён из `useSupabaseCheckpoints.js`/`useCheckpoints.js`. `vault/03-База-данных/checkpoints.md`/`scan_packets.md` дополнены разделом «Realtime». `npm run build` проходит. **Применение миграции к боевой БД и ручная сквозная проверка на устройстве — не выполнены**, см. [IMPLEMENTATION_REPORT.md](143/IMPLEMENTATION_REPORT.md). Ревью: [REVIEW_REPORT.md](143/REVIEW_REPORT.md) — PASS (диагностика и SQL-фикс независимо перепроверены и корректны; Minor: гипотеза подтверждена только по дампу, не запросом к живой БД, вторичные гипотезы — стабильность WebSocket после простоя вкладки и отсутствие try/catch в `load()` — не проверялись; критерии приёмки формально не закрыты до применения миграции пользователем) | DONE |
| [146](146/146.txt) | UI/UX-аудит (только дизайн, без изменения кода): страница `/admin/checkpoints/bus/12` (`CheckpointBusView.vue`) неоднородна со стилем `/admin/checkpoints` и показывает почти одинаковые числа детей/воспитателей в двух соседних блоках. Разобрано в [AUDIT_REPORT.md](146/AUDIT_REPORT.md): дублирование реальное, но не тождественное — один блок сравнивает с Baseline (первый закрытый Checkpoint дня), другой — с полным ростером автобуса, поэтому просто "оставить подробный блок" по совету заказчика решает проблему лишь при осознанном выборе одной шкалы сравнения. Три самодостаточных HTML-прототипа на синтетических данных (`file://`, без сервера): [concept-1-unified-summary.html](146/concept-1-unified-summary.html) (консервативный — обе метрики в одной карточке с поясняющими подписями), [concept-2-kpi-dashboard.html](146/concept-2-kpi-dashboard.html) (KPI-плитки + новая метрика "6 из 8 автобусов отчитались" + bottom-sheet для сканов, единая шкала сравнения — ростер), [concept-3-bus-focused.html](146/concept-3-bus-focused.html) (сводка уходит в узкую sticky-панель, сетка автобусов — главный элемент экрана). Рекомендация — концепт 2 как компромисс риска/пользы | AUDIT_DONE |
| [150](150/150.txt) | UI: `ArmbandView.vue` — список выбора ребёнка в "Armband-Zuordnung" выводит имя/возраст/ID браслета одной строкой, плохо читается на телефоне; нужно разбить на несколько строк (имя / Alter / Armband Id) | NEW |
| [151](151/151.txt) | Объединить `ChildDetailView.vue` (`/child/:id`) и `ChildCardView.vue` (`/admin/checkpoints/child/:id`) — два почти идентичных экрана просмотра ребёнка — в один, за основу взять `ChildCardView.vue` (уже есть история сканов + стиль кнопок `CheckpointBusView.vue`). Требуется: фильтр истории сканов только за сегодня (`scans.date`, сейчас — все сканы без ограничения по дате, только `limit(50)`); кнопка "Zurück" должна возвращать на реальный предыдущий экран (`router.back()`, сейчас в `ChildDetailView.vue` жёстко `/main`); стиль/паддинги кнопок как в `CheckpointBusView.vue` (компактная "Zurück", увеличенные отступы у остальных кнопок, кроме "Entfernen" — она должна оставаться маленькой, и её нужно добавить в объединённый экран, сейчас у `ChildCardView.vue` такой кнопки нет вообще) | NEW |
| [152](152/152.txt) | Scanner-Einstellungen (`ScannerSettingsView.vue`, `/scanner-settings`) не показывает видеопоток выбранной камеры — подтверждено чтением кода: на странице нет вообще никакого `<video>`/превью-контейнера, `Html5Qrcode.getCameras()` только перечисляет устройства. Не регресс, а нереализованная функциональность. Требуется полноценный QR-сканер на странице настроек (по образцу `Scanner.vue`, `Html5Qrcode` + рамка распознавания), с корректным стартом/остановкой потока при смене камеры и уходе со страницы; при реализации свериться с фиксом зависания `resume()` из тикета 141 | NEW |

