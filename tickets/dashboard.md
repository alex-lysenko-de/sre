# Дашборд тикетов

Обзорная доска, показывающая порядок выполнения тикетов. После создания нового тикета или смены его статуса — обновлять эту таблицу.

| № | Название | Статус |
|---|----------|--------|
| [101](101/101.txt) | Config: ошибка доступа "keine Rechte" для Hauptbetreuer | DONE |
| [102](102/102.txt) | Days-edit: удаление дня не сохраняется в БД (появляется снова после reload) | DONE |
| [103](103/103.txt) | Scanner: выбор камеры на iPhone (селфи вместо основной) + громкость/вибрация/визуальный фидбек | DONE |
| [104](104/104.txt) | AdminBusView (/admin-busses): упрощение интерфейса, отступы, шрифты | DONE |
| [105](105/105.txt) | Миграция хранения авторизации с Local Storage на LocalForage | CHANGES_REQUIRED |
| [106](106/106.txt) | Kopfzählung (Headcount) для "Meine Gruppe" | FIXES_APPLIED |
| [107](107/107.md) | Ревизия и уборка файлов проекта / документации | AUDIT_DONE |
| [108](108/108.md) | База знаний проекта (Obsidian vault) | DONE |
| [109](109/109.md) | Пользовательская документация | не начат |
| [110](110/110.md) | Миграция в сторону multi-tenant (план) | не начат |
| [111](111 — DB backup tooling, коммит 17e206d, без файла в tickets/) | Backup-тулинг для БД (pg_dump schema+data) | DONE (только в git-истории) |
| [112](112/112.txt) | Сохранение авторизации в PWA на iPhone (Safari / Add to Home Screen) | DEVELOPMENT_DONE |
| [114](114/114.txt) | Bug fix: редирект на /info вместо home page после логина/регистрации | DONE |
| [115](115/115.txt) | Bug fix: работа с группой "null" | DONE |
| [113](113/113.txt) | Автоматическое создание PWA-приложения | CHANGES_REQUIRED |
| [117](117/117.txt) | LocalForage ↔ Supabase: архитектура обмена данными | тикет создан |
| [116](116/116.txt) | UI-прототип нового сканера (для будущей функциональности тикета 120; существующий `/scanner` не меняется). Решения зафиксированы в [tickets/116/DECISIONS.md](116/DECISIONS.md). Реализовано по [tickets/116/IMPLEMENTATION_PLAN.md](116/IMPLEMENTATION_PLAN.md) — 2 слоя (`Scanner.vue` + `ScannerPrototypeView.vue`), новый маршрут `/scanner-prototype`, кнопка "Отправить" — заглушка. По итогам ревью и ручного тестирования на устройствах исправлены: различение ошибки сети vs "браслет не найден", квадратная область сканера, единая кнопка выбора камеры, длительность экрана подтверждения по исходу, повтор скана = успех (не ошибка), сброс раунда + результат "Senden" в области сканера (см. `IMPLEMENTATION_REPORT.md`). Повторное ревью — PASS (`REVIEW_REPORT.md`); визуальную повторную проверку на реальных устройствах (Samsung/iPhone) рекомендуется выполнить перед UX-решением по 120 | DONE |
| [118](118/118.txt) | Рефакторинг документации: уборка doc/ после базы знаний + компактная сводка для ИИ (CLAUDE.md). Backlog нереализованных идей AdminBusView, спасённый из удалённого `doc/AdminBusView_TechTask.md`: [tickets/118/AdminBusView_Backlog.md](118/AdminBusView_Backlog.md) | FIXES_APPLIED |
| [120](120/120.txt) | Модуль перекличек: 3 подрежима сканирования (автобус/группа/свободные отметки) поверх базового сканера, пакетная отправка результатов (PresencePacket). `120.txt` переработан в единый актуальный источник требований (архив: `DECISIONS.md`, старый `implementation_plan.md`). Реализовано по [tickets/120/IMPLEMENTATION_PLAN.md](120/IMPLEMENTATION_PLAN.md) — новый `useScanPacket.js` (сборка/отправка пакета) + три View (`ScannerBusView`/`ScannerGroupView`/`ScannerCheckinView`), переиспользующие `Scanner.vue` из 116 без изменений; маршруты `/scanner/bus|group|checkin`; `ScannerView.vue`/`ScannerPrototypeView.vue` и `useArmband.recordChildPresence()` удалены. По итогам ревью (`REVIEW_REPORT.md`, CHANGES_REQUIRED) исправлено: `finished_at` больше не пересчитывается при повторной отправке, ошибки отправки/загрузки ростера показывают реальный текст ошибки и кнопку повтора; риск рассинхронизации звук/вибрация vs визуальный результат при скане "чужой" группы в GROUP — осознанно принят и задокументирован (контракт `Scanner.vue` не меняется в рамках 120). Сквозная проверка на реальных устройствах против задеплоенной `submit-scan-packet` по-прежнему отложена до совместного релиза с 122 (см. `IMPLEMENTATION_REPORT.md`) | BUGFIX_DONE |
| [121](121/121.txt) | Research: сравнение архитектурных вариантов серверной обработки пакетов сканирования (подготовка к 122). Результат: [tickets/121/IMPLEMENTATION_PLAN.md](121/IMPLEMENTATION_PLAN.md) — вариант A (заголовок пакета + переиспользование `scans`) с обязательной пакетной переработкой триггеров (устранение N-кратных каскадов), Edge Function `submit-scan-packet`, идемпотентность по `client_packet_id`, схема заранее совместима с будущей отменой пакета | ARCHITECT_DONE |
| [122](122/122.txt) | Серверная логика обработки пакетов сканирования (приём/хранение `scan_packets`, идемпотентность, батч-триггеры, UI администратора) — реализуется одновременно с 120, на основе research-а 121. Реализовано по [tickets/122/IMPLEMENTATION_PLAN.md](122/IMPLEMENTATION_PLAN.md): SQL в [doc/db/scan_packets.sql](../doc/db/scan_packets.sql) (таблица `scan_packets`, `scans.packet_id`/`method`, батч-триггеры на transition tables для `on_scan_insert`/`on_children_today_change`, функция `submit_scan_packet()`, RLS), Edge Function `supabase/functions/submit-scan-packet`, композабл `useScanPackets.js` (админ-чтение), пакеты видны в `BusDetailModal.vue` и новом `GroupDetailModal.vue` (вход из `ChildrenView.vue`). По итогам ревью (`REVIEW_REPORT.md`) исправлено: батч-триггер и `submit_scan_packet()` теперь явно `RAISE EXCEPTION` на невозможных состояниях данных («ребёнок без группы», «элемент `children[]` без `child_id`») вместо тихого молчания — по решению автора задачи такие случаи не «обрабатываются», а считаются нарушением инварианта. Баг «Fehler beim Senden, failed to fetch» диагностирован и устранён: SQL уже был применён к БД, но Edge Function ни разу не была задеплоена (`404 NOT_FOUND`, из-за чего браузер видел провал CORS-preflight как `Failed to fetch`) — задеплоена (`npx supabase functions deploy submit-scan-packet`), проверена (`OPTIONS`→200). Регрессия по `HeadcountView`/`ChildrenView`/`AdminBusView`/`ChildDetailView` и сквозная проверка с 120 на устройствах — по-прежнему не выполнены (см. `IMPLEMENTATION_REPORT.md`) | BUGFIX_DONE |
| [123](123/123.txt) | ~~Редизайн интерфейса Kopfzählung~~ — фича признана неудачным экспериментом и полностью удалена из приложения по прямому запросу пользователя (не дошла до ручной проверки на устройстве, см. предыдущий статус `DEVELOPMENT_DONE`): удалены `src/views/HeadcountView.vue`, `src/composables/useGroupScanSession.js`, маршрут `/headcount`, кнопки "Kopfzählung"/"Headcount" в `MainView.vue`/`GroupEditView.vue` (включая `canShowHeadcountButton`/`goToHeadcount`). `Scanner.vue` очищен от плумбинга, добавленного только ради встроенной панели Kopfzählung (126): проп `autoStart`, состояние "камера выключена"/кнопка "Scan" внутри области, emits `camera-started`/`camera-stopped`, `start()`/`stop()` в `defineExpose` — камера теперь всегда стартует на `onMounted`, как до 126. `npm run build` проходит | REMOVED |
| [124](124/124.txt) | Клиентское логирование ошибок + версионность приложения. План: [tickets/124/IMPLEMENTATION_PLAN.md](124/IMPLEMENTATION_PLAN.md) — модуль `clientLog.js` (ring-buffer breadcrumbs, перехват `errorHandler`/`window.onerror`/`unhandledrejection`, патч `console.error`/`warn` вместо правки composables, Pinia-плагин для breadcrumbs действий), таблица `client_logs` (BEFORE INSERT триггер проставляет `user_id` из `auth.uid()`, RLS — только INSERT, без SELECT из приложения), версия из `package.json` через `vite.config.js` `define`, новый экран `DiagnosticsView.vue` (версия + локальный лог + Web Share/копирование). Toast/push/чат — намеренно не входят, отдельная схема не создаётся под них. Ревью ([REVIEW_REPORT.md](124/REVIEW_REPORT.md)) — реализация фактически отсутствует (нет `IMPLEMENTATION_REPORT.md`, ноль изменений в затронутых планом файлах) | CHANGES_REQUIRED |
| [126](126/126.txt) | UI-доработки навигации и сканера: кнопка-логотип в навбаре → явная кнопка Home; "Ich fahre heute mit!" переносится из навбара на Willkommen; кнопки пересчёта скрыты, пока `isCheckInRequired`; настройки камеры сканера убраны из рабочих экранов в новый пункт меню «Сканер»; в Kopfzählung (123) панель сканера больше не схлопывается — выключается только камера; кнопка "Reset" → "Close" на экранах Bus/Group/Checkin; несвязанный бейдж предлагает Cancel/Привязать бейдж (переход на существующий `/armband/:id`) вместо авто-закрытия. Реализовано по [tickets/126/IMPLEMENTATION_PLAN.md](126/IMPLEMENTATION_PLAN.md) ([IMPLEMENTATION_REPORT.md](126/IMPLEMENTATION_REPORT.md)): новый composable `useScannerSettings.js` (камера/длительности, вынесены из `Scanner.vue`, миграция старого `localStorage`-ключа) + `ScannerSettingsView.vue`; `Scanner.vue` получил `autoStart`, `start()`, emits `camera-started`/`camera-stopped`, опциональный `onBindRequested` (Kopfzählung его не использует — открытый в 126.txt вопрос решён явно); `useGroupScanSession.js`/`HeadcountView.vue` переведены с монтирования панели на управление камерой через `scannerRef` и события. `npm run build` проходит без ошибок; ручная проверка в браузере/на устройстве не выполнена (нет инструмента браузерной автоматизации в этой сессии) — отложена, как и у 120/122/123. По итогам ревью (`REVIEW_REPORT.md`, CHANGES_REQUIRED) — критическая ошибка: в `Scanner.vue` `isTransitioning` сидировался значением `autoStart` до `onMounted`, из-за чего собственный guard `startCamera()` блокировал первый автозапуск камеры — Bus/Group/Checkin (режим `autoStart=true` по умолчанию) не запускали камеру вообще, виден был только вечный спиннер; Kopfzählung (`autoStart=false`) не затронут. Исправлено: `isTransitioning` теперь всегда стартует с `false`; также применены необязательные правки ревью (try/catch в `handleConfirmationBind`, документирующий комментарий про неиспользуемый `start()`). После ручного тестирования обнаружено, что навбар без кнопки "Ich fahre heute mit!" не помещается в одну строку (кнопка вызова меню переносится, большой промежуток между Home и Gruppe) — по решению пользователя навбар **откачен** к состоянию до 126 (лого снова "🌳 SRE" вместо иконки+"Home"; пункт меню "📷 Scanner" сознательно оставлен), кнопка "Ich fahre heute mit!" при этом не восстановлена нигде (ни в навбаре, ни на Willkommen) — убрана из отображения полностью, `DailyCheckInModalView.vue` остаётся в коде, но больше нигде не подключается. Одновременно с этим (см. отдельную запись) удалена Kopfzählung (123), поэтому весь плумбинг, добавленный в `Scanner.vue` только ради её встроенной панели, тоже убран. `npm run build` проходит. Ручная проверка на устройстве по-прежнему не выполнена | BUGFIX_DONE |
| [130](130/130.txt) | Редизайн интерфейса администратора для переклички, сущность переименована в «Контрольная точка» (Checkpoint): `AdminBusView.vue`/`ChildrenView.vue` сейчас смешивают статистику/Aktualisieren/Tag starten("Hard Reset")/Soft Reset/Bus Alerts на разрозненных экранах. Итоговая модель — гибридная, зафиксирована отдельно в [tickets/130/decision.md](130/decision.md) после отказа от двух более ранних вариантов (явный create/finish с отказом пакета при закрытой перекличке; затем — "конец = начало следующей"): контрольная точка создаётся **автоматически** по первому пакету своего типа (воспитатель никогда не блокируется и не получает отказ), завершается **только явно** администратором, типы (Bus/Group/Lazy) независимы и могут быть открыты одновременно — конфликт только между двумя открытыми одного типа. Архитектурный план ([tickets/130/IMPLEMENTATION_PLAN.md](130/IMPLEMENTATION_PLAN.md), приведён в соответствие с `decision.md`): таблица `checkpoints` (unique `(day, type) WHERE status=1`), `scan_packets.checkpoint_id`, `submit_scan_packet()` — авто-создание через `INSERT ... ON CONFLICT DO NOTHING`, RPC `create_checkpoint`/`finish_checkpoint`/`cancel_checkpoint`, автоматическая граница дня без `pg_cron` (date-scoped `children_today`/`groups_today`) + baseline-снимок при первой завершённой точке дня; экраны `CheckpointListView`/`CheckpointBusView`/`CheckpointGroupView`/`CheckpointLazyView`. Открытые риски: отсутствие механизма отмены уже принятых пакетов при `cancel_checkpoint()`, расширение ответственности недоверенного `submit_scan_packet()` (теперь тоже пишет в `checkpoints`), RLS `checkpoints` должна избежать legacy-widening ошибки `reset_events`. БД/код не применялись. Попытка реализации в этой же сессии остановлена ([IMPLEMENTATION_REPORT.md](130/IMPLEMENTATION_REPORT.md)): и `130.txt`, и `IMPLEMENTATION_PLAN.md` явно требуют, что БД/код меняются только в отдельном тикете реализации после утверждения концепции (аналог 121→122) — тикет реализации заведён отдельно, см. [131](131/131.txt) | REQUIREMENTS_CLARIFICATION |
| [131](131/131.txt) | Тикет реализации для 130 (аналог связки 121→122): применяет к БД и клиентскому коду концепцию «Контрольная точка» (Checkpoint) — на основе `decision.md`/`IMPLEMENTATION_PLAN.md` из 130 **и** уже согласованного с пользователем сущностного UI-прототипа 130_2 (заменяет мок реальными данными, а не переделывает экраны заново). Архитектурный план — [tickets/131/IMPLEMENTATION_PLAN.md](131/IMPLEMENTATION_PLAN.md): таблица `checkpoints` **без CANCELLED** (только OPEN/FINISHED — по явному указанию пользователя "Remove" теперь физически удаляет checkpoint и каскадно её `scan_packets`/`scans`, реализовано процедурно в `remove_checkpoint()`, не через FK `ON DELETE CASCADE`), RPC `create_checkpoint`/`finish_checkpoint`/`reopen_checkpoint`/`remove_checkpoint` (`SECURITY DEFINER`, admin+active-проверка, `search_path` зафиксирован — первое использование Postgres RPC в проекте), расширение `submit_scan_packet()` (авто-создание/поиск, без отказов пакету). Реализовано ([IMPLEMENTATION_REPORT.md](131/IMPLEMENTATION_REPORT.md), Phase 1): SQL-файлы подготовлены в `doc/db/` (**не применены к боевой БД**), `useSupabaseCheckpoints.js`/`useCheckpoints.js`/`useLazyCheckpointProgress.js`/`useGroupEntity.js` (те же имена функций, что и мок), все 8 реальных экранов перенесены из прототипа `checkpoints-prototype/`, новые маршруты `/admin/checkpoints/*` добавлены рядом с `/admin-busses`/`/children` (не заменяют их — Phase 2). `npm run build` проходит. Явные границы: SQL не применён, устройство не тестировано, `children_today`/`groups_today` date-scoped миграция и удаление старых экранов — отдельный Phase 2 | DEVELOPMENT_DONE (Phase 1) |
| [130_2](130_2/130_2.txt) | UI-прототип экрана управления контрольными точками на mock-данных — по аналогии с тем, как 116 был прототипом сканера перед 120. Изолированный новый экран (например, `/admin/checkpoints-prototype`), список/создание/просмотр контрольных точек, переключение типов Bus/Group/Lazy, синтетические результаты и все состояния (идёт/завершена/ошибка/нет детей) — целиком в локальном массиве/store, без БД, без сети, без изменения существующей логики. Цель — согласовать UX до того, как под него пишется боевая реализация в 131; после согласования интеграция сведётся к замене источника данных на реальный API. Архитектурный план ([tickets/130_2/IMPLEMENTATION_PLAN.md](130_2/IMPLEMENTATION_PLAN.md)): mock-composables `useCheckpointsMock.js`/`useLazyCheckpointProgressMock.js` (module-level reactive singleton, не Pinia-стор — намеренно, чтобы не выглядеть боевым доменом) повторяют сигнатуры будущего `useCheckpoints.js`/`useLazyCheckpointProgress.js` из 130-плана, чтобы 131 мог заменить только источник данных; новые изолированные маршруты `/admin/checkpoints-prototype(/bus\|group\|lazy/:id)`, экраны `CheckpointListPrototypeView`/`CheckpointBusPrototypeView`/`CheckpointGroupPrototypeView`/`CheckpointLazyPrototypeView` (суффикс `Prototype` — во избежание коллизии с зарезервированными за 131 "чистыми" именами), presentational-компоненты `CheckpointStatusBadge`/`CheckpointTypeBadge`/`CheckpointOriginBadge`/`CheckpointCreateModal`; seed-сценарий намеренно включает демонстрацию нормального состояния "два открытых типа параллельно" и аварийного "две открытые одного типа" (только seed, недостижимо через UI); сознательно не читает `useConfigStore` (`total_groups`/`total_buses`), чтобы не тянуть сетевую зависимость — количество автобусов/групп захардкожено в моке. Реализовано по плану ([IMPLEMENTATION_REPORT.md](130_2/IMPLEMENTATION_REPORT.md)): все файлы из плана созданы как есть, все обязательные состояния DoD демонстрируются через seed+UI, `npm run build` проходит (4 новых lazy-loaded чанка), ни один новый файл не импортирует `@/supabase`. Единственное отклонение — `created_by`/`finished_by`/`cancelled_by` в моке хранят объект `{id,name,isAdmin}` вместо голого `bigint` (нет настоящей таблицы `users`), сигнатуры/имена функций не затронуты. Ручная проверка в браузере (Network-панель пуста) не выполнена — экран защищён admin-guard'ом против реального продуктивного Supabase-проекта, а вход под бутстрап-admin-учётными данными сознательно не выполнялся без явного запроса пользователя. После первичной реализации прошло 4 раунда UX-обратной связи (см. [IMPLEMENTATION_REPORT.md](130_2/IMPLEMENTATION_REPORT.md), раздел «UX-Feedback и дальнейшая доработка»): Runde 1 — таблицы → карточки, "Cancel" → reopen/remove; Runde 2 — уплотнение карточек, аккордеон для групп/автобусов; Runde 3 — карточки/шапки показывают итог и отклонение от реальной (не захардкоженной) базы дня, предупреждение перед закрытием при открытых проблемах, копируемые списки присутствующих/отсутствующих, багфикс аккордеона групп; Runde 4 (крупнейший, архитектурный) — переход от экранного к сущностному дизайну: канонические ростеры детей (id 1-24, единый пул для Bus/Group/Lazy)/Betreuer (id 101-112) с выдуманными, но детерминированными доп.полями, новые сквозные сущности "история сканирований ребёнка" и "карточка группы" (по дню, не по точке), универсальный список детей/Betreuer и переиспользуемые Link-компоненты, новые экраны/маршруты (`/list`, `/child/:id[/edit]`, `/betreuer/:id`, `/group-entity/:id`), Bus/Group/Lazy-экраны переведены на клик-переходы вместо инлайновых раскрывающихся панелей. `npm run build` проходит после каждого раунда; ручная проверка в браузере ни разу не выполнялась (нет инструмента браузерной автоматизации) | DEVELOPMENT_DONE |
